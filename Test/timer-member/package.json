{
  "name": "qa-timer-member",
  "private": true,
  "version": "1.0.0",
  "description": "Standalone Playwright automation suite for the Member Timer (/timer) QA workflow (not part of the pnpm workspace).",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "dotenv": "^16.4.5"
  }
}
